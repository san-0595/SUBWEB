# Cloudflare Pages 配置文件
# 用于配置构建和函数设置

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[functions]]
  pattern = "/api/*"

[functions.env]
  # 在这里配置环境变量
  # MY_SECRET = "your-secret-value"